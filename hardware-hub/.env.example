# URL web app Asihjaya RMS di cloud/VPS.
# Local dev: http://localhost:3000
# Production: https://app.asihjaya.com
ASIHJAYA_API_URL=http://localhost:3000

# Dibuat dari root web app dengan:
# npm run hardware:agent:create
HARDWARE_AGENT_ID=cd5a598a-69fe-4d78-bf85-4a819c24715d
HARDWARE_AGENT_SECRET=asihjaya-local-agent-secret-1234567890

# Mode development tanpa hardware fisik.
# true  = job tetap diklaim dan ditandai completed, output disimpan ke folder dry-run.
# false = agent benar-benar mengirim perintah ke printer/cash drawer.
HARDWARE_DRY_RUN=false
DRY_RUN_OUTPUT_DIR=./dry-run-output

# Printer lokal Windows di Mini PC outlet.
# Isi dengan SHARE NAME printer Windows, bukan hanya display name kalau berbeda.
LABEL_PRINTER_NAME=SATO CG408TT
DOCUMENT_PRINTER_NAME=
CASH_DRAWER_PRINTER_NAME=

# Layout label SATO.
# jewelry_compact = layout awal untuk label perhiasan kecil.
LABEL_PROFILE=jewelry_compact
LABEL_COPIES=1
LABEL_LEFT_OFFSET_DOTS=0
LABEL_TOP_OFFSET_DOTS=0
LABEL_INCLUDE_PRICE=false

# Untuk silent print PDF, rekomendasi pakai SumatraPDF di Windows.
# Contoh:
# PDF_PRINT_COMMAND="C:\\Program Files\\SumatraPDF\\SumatraPDF.exe" -print-to "{printer}" -silent "{file}"
PDF_PRINT_COMMAND=

POLL_INTERVAL_MS=2000
HEARTBEAT_INTERVAL_MS=30000

# Operational hardening.
# REQUEST_TIMEOUT_MS = timeout request agent ke web app/API.
# PRINT_COMMAND_TIMEOUT_MS = timeout command cetak lokal Windows/SumatraPDF.
REQUEST_TIMEOUT_MS=15000
PRINT_COMMAND_TIMEOUT_MS=60000
